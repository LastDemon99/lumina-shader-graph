import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const polarCoordinatesNode: NodeModule = {
  type: 'polarCoordinates',
  definition: NODE_DEFINITIONS.polarCoordinates,
};
