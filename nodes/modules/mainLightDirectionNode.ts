import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const mainLightDirectionNode: NodeModule = {
  type: 'mainLightDirection',
  definition: NODE_DEFINITIONS.mainLightDirection,
};
