import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const vector4Node: NodeModule = {
  type: 'vector4',
  definition: NODE_DEFINITIONS.vector4,
};
