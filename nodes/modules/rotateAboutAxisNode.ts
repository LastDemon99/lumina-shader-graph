import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const rotateAboutAxisNode: NodeModule = {
  type: 'rotateAboutAxis',
  definition: NODE_DEFINITIONS.rotateAboutAxis,
};
