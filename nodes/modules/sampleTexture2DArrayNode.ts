import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const sampleTexture2DArrayNode: NodeModule = {
  type: 'sampleTexture2DArray',
  definition: NODE_DEFINITIONS.sampleTexture2DArray,
  ui: {
    sections: [],
  },
};
