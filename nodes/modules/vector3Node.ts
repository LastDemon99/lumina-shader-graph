import { NODE_DEFINITIONS } from '../../constants';
import type { NodeModule } from '../types';

export const vector3Node: NodeModule = {
  type: 'vector3',
  definition: NODE_DEFINITIONS.vector3,
};
